
# 生产环境配置

# 环境变量
# 环境配置文件会在指定模式中被载入

# 环境变量以键值方式定义, 且必须使用 VUE_APP_ 作为前缀，
# 环境变量访问：可通过全局 process.env 在任何地方使用

# 配置NODE_ENV环境变量，不可轻易重写NODE_ENV变量，因为打包时部分操作依赖于此变量，如：CDN配置
# NODE_ENV = "development"
NODE_ENV="production"

# 环境名称
VUE_APP_ENV_NAME="OFFLINE"

# 打包输出目录
VUE_APP_OUTPUT_DIR="dist"

# 环境路径
VUE_APP_ENV_PATH="/"

# 基础模块接口基地址
VUE_APP_BASE_URL="http://192.168.10.115:9083"

# 业务模块接口基地址
VUE_APP_BUSINESS_URL="http://192.168.10.115:9084"
