```shell
# 配置Git用户名
git config --global user.name <你的用户名>
git config --global user.email <你的邮箱>

# 克隆最近一次提交
git clone git@e.coding.net:jw_/web/web-dev.git --depth=1

# 切换分支
git checkout <分支名称>

# 列表所有分支，*号表示当前分支
git branch

# 放弃本地所有修改
git checkout .

# 查看文件状态
git status
```